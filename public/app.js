const form=document.querySelector('#rsvp-form'), attendees=document.querySelector('#attendees-field'), error=document.querySelector('.form-error');
document.querySelectorAll('input[name="attending"]').forEach(input=>input.addEventListener('change',()=>{attendees.hidden=input.value!=='yes'||!input.checked; attendees.querySelector('input').required=!attendees.hidden;}));
form.addEventListener('submit',async e=>{e.preventDefault(); error.textContent=''; if(!form.reportValidity())return; const data=Object.fromEntries(new FormData(form)); data.attending=data.attending==='yes'; data.attendees=Number(data.attendees); const button=form.querySelector('button'); button.disabled=true; button.textContent='Enviando…'; try{const r=await fetch('/api/respond',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),out=await r.json(); if(!r.ok)throw Error(out.error); form.hidden=true; const thanks=document.querySelector('#thanks'); thanks.hidden=false; thanks.querySelector('h2').textContent=out.attending?'¡Gracias!':'Gracias por avisarnos.'; thanks.querySelector('p').textContent=out.attending?'Nos hace mucha ilusión compartir este día contigo.':'Sentimos que no puedas acompañarnos; esperamos compartir muchos momentos más contigo.';}catch(err){error.textContent=err.message||'No se ha podido enviar la confirmación. Inténtalo de nuevo.'; button.disabled=false;button.textContent='Enviar confirmación';}});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
